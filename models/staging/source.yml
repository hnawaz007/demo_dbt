version: 2

sources:

  - name: src_sql # name of the source 
    schema: warehouse # this is the schema our raw data lives in
    database: dbt # this is the name of our database

    quoting:
      database: false
      schema: false
      identifier: false

    tables:
      - name: states
        description: >
          Contains US State Name and Code
        columns:
          - name: StateCode
            description: The primary key for this table
            tests:
              - unique
              - not_null
          